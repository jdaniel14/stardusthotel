@{
    ViewBag.Title = "ListDetalle";
}

<h2>Listar Detalle</h2>

@model IEnumerable<Stardust.Models.horariodetalle>

@{
    ViewBag.Title = "Lista de Detalles del horario;
}

<h2>Horario </h2>



<table cellpadding = "0" cellspacing = "0" width = "100%" class = "sTable">
     
    <thead>
        <tr>
            
            <td>Día de Semana </td>
            <td>Hora de entrada</td>
            <td>Hora de Salida</td>
            <td>Opciones</td>
        </tr>
    </thead>

    <tbody>
    @foreach (var item in Model) {
        <tr> 
        @*
            <td>
               <a href="@Url.Action("CreateHorario", new { id = item.idempleado })"><img src="../../Content/images/icons/color/application-plus-black.png", alt="Create" /></a>
            </td>
        *@
            @* <td>@Html.DisplayFor( modelItem => item.nombreEmpleado )</td> *@
            
           
            <td>@Html.DisplayFor(modelItem => item.diasemana)</td>
			<td>@Html.DisplayFor(modelItem => item.horasentrada)</td>
			<td>@Html.DisplayFor(modelItem => item.horasSalida)</td>
			
            <td>
				<a href="@Url.Action("Editar", new { id = item.idhorariodetalle })"><img src="../../Content/images/icons/color/pencil.png", alt="Edit" />Editar </a
			</td>
        </tr>
        
    }
    </tbody>
</table>